.backgroundWrapper.process
  .grid.spaced.centered
    %header.pageBanner
      %h1 Blog
- if current_user
  = link_to 'New Blog Post', new_admin_user_blog_path(current_user)
= paginate @blogs
= render @blogs