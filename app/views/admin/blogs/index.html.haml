- content_for :admin_actions do
  = link_to 'New Blog Post', new_admin_user_blog_path(current_user), class: 'button primary'
.layout-list
  = render partial: 'admin/blogs/preview', collection: @blogs, as: :blog
  = paginate @blogs